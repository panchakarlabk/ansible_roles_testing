---
- name: Include a role from a collection
  hosts: all
  gather_facts: false
  #become: true
  tasks:
    - name: Testing Web Httpd role
      ansible.builtin.include_role:
        name: /home/ansible/test/web/
        tasks_from: main.yml
